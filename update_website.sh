#!/bin/sh
# A bash script for updating my github pages website

# Add everything to your local git repository and push it up to your remote repository on GitHub:
git add .
git commit -m "Initial commit"
git push -u origin master

exit 0
